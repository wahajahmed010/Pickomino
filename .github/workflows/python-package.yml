# This workflow installs Python dependencies, run tests and lint with a variety of Python versions.
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:

    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
        python-version: [ "3.10", "3.11", "3.12", "3.13", "3.14" ]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.SUBMODULE_PAT }}
          persist-credentials: false
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test,dev]"
          pip install pyright==1.1.408 ruff==0.14.13
      - name: Lint with ruff
        run: ruff check . --exclude tests/,examples/
      - name: Type check with Pyright
        run: pyright --project=pyproject.toml
      - name: Run pytest with coverage
        run: pytest --cov=pickomino_env --cov-report=term-missing
